## 📜 โพยคำสั่ง Terminal (Command Cheat Sheet)

### 1. เริ่มงาน / เปิดร้าน 🚀

- **`npm run dev`**
  - **ใช้ตอนไหน:** ทุกครั้งที่จะเริ่มเขียนโค้ด หรือดูหน้าเว็บ
  - **ทำอะไร:** เปิด Server ของเว็บ (`localhost:3000`)

### 2. จัดการข้อมูล (Database) 🗄️

- **`npx prisma studio`**
  - **ใช้ตอนไหน:** อยากดู/เพิ่ม/ลบ ข้อมูลใน Database แบบง่ายๆ (เหมือน Excel)
  - **ทำอะไร:** เปิดหน้าเว็บจัดการข้อมูล (`localhost:5555`)
- **`npx prisma db push`**
  - **ใช้ตอนไหน:** _เฉพาะตอน_ แก้ไฟล์ `schema.prisma` (เช่น เพิ่มตารางใหม่, เพิ่มคอลัมน์)
  - **ทำอะไร:** อัปเดตโครงสร้าง Database บน Cloud ให้ตรงกับไฟล์ในเครื่อง

### 3. แก้ปัญหา / ซ่อมแซม 🔧

- **`npx prisma generate`**
  - **ใช้ตอนไหน:** เมื่อรันเว็บแล้วเจอ Error ว่าหา Prisma ไม่เจอ หรือหลังจากแก้ `schema.prisma`
  - **ทำอะไร:** สร้างตัวแปลภาษา (Client) ใหม่ ให้โค้ดรู้จัก Database ล่าสุด
- **`npm install`**
  - **ใช้ตอนไหน:** เพิ่งโหลดโปรเจกต์มาใหม่ หรือเผลอลบโฟลเดอร์ `node_modules`
  - **ทำอะไร:** ติดตั้งเครื่องมือทุกอย่างที่จำเป็นให้ครบ

### 4. การติดตั้งของใหม่ 📦

- **`npm install [ชื่อแพ็กเกจ]`**
  - **ตัวอย่าง:** `npm install lucide-react`
  - **ใช้ตอนไหน:** อยากได้ลูกเล่นเสริมมาใส่ในเว็บ
